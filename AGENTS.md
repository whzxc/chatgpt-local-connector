# 开发约定

- 采用 MIT 许可证。公开源码与桌面产物在 GitHub 维护。推送与发布必须获得用户授权；本项目不发布公共 npm 包。
- 使用 macOS 与 Windows；业务代码不依赖其他项目或个人环境。采用 Tauri + Rust 原生桌面，Node/npm 仅用于开发构建；本机 Tunnel Client 的安装、配置与生命周期由产品管理，Tunnel 身份申请和服务端仍使用官方方案。
- 不使用 Git worktree，不保留旧实现的 alias、shim 或迁移兼容层。
- 不新增测试文件或用例；迁移已有测试不扩大覆盖。运行已有检查和真实验收。
- 提交信息使用 Conventional Commits：`type(scope): 中文说明`，单行不写 body。
- 私人路径、账号、密钥、Tunnel 身份及真实任务内容不进入版本库。
- 文档只描述当前功能、架构、限制和操作方法，不保留过程记录、验收结果、决策日志或个人环境信息。

## UI 组件约定

- 所有单选控件统一使用 `SingleChoice`：选项数 ≤ 5 且宽度足够时使用胶囊选项卡；> 5 或宽度不足时使用下拉选择。禁止在业务页面直接新增 `NSelect`、原生 `select` 或重复实现单选样式；主题统一配置，保留键盘导航、禁用状态与表单校验。

- 通用表单、输入、选择、开关、按钮、弹窗和提示使用 Naive UI，按需命名导入；不在业务页面重新手写同类控件。
- 主题统一在 `ui/components/UiProvider.vue` 配置，深浅色状态使用 `ui/theme.ts`；禁止用全局元素选择器或深层选择器覆盖组件库内部样式。
- 编辑弹窗复用 `FormDialog`；表单使用 `NForm` / `NFormItem`，默认单列、标签在上，仅短且相关的字段可并排。校验错误归属字段，折叠项不能绕过校验。
- 首页拓扑、主按钮等定制视觉可以保留自绘；现有布局组件只管排列，不再复制输入框和按钮样式。详细规范见 `docs/ui.md`。
